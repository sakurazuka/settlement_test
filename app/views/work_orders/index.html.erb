<h1>Listing work_orders</h1>

<%= search_form_for @q, url: work_orders_path do |f| %>
  <%= select_tag  'settlement', options_for_select([["", 0], ["mikessan", 1], ["kesssanzumi", 2]], params[:settlement]) %>
  <%= f.submit %>
<% end %>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @work_orders.each do |work_order| %>
      <tr>
        <td><%= work_order.code %></td>
        <td><%= work_order.name %></td>
        <td><%= link_to 'Show', work_order %></td>
        <td><%= link_to 'Edit', edit_work_order_path(work_order) %></td>
        <td><%= link_to 'Destroy', work_order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Work order', new_work_order_path %>
